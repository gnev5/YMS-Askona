# Резервное копирование и восстановление базы данных

## Описание

Этот проект включает скрипты для безопасного резервного копирования и восстановления базы данных PostgreSQL в Docker-контейнере.

## Скрипты

### `backup_database.py`
Создает резервную копию текущей базы данных.

**Что делает:**
- Проверяет, что контейнер БД запущен
- Создает дамп всех данных в формате SQL
- Сохраняет файл в директорию `backups/`
- Создает сжатую копию (.gz) для экономии места
- Показывает размер файлов

**Использование:**
```bash
python backup_database.py
```

**Пример вывода:**
```
💾 Создание резервной копии базы данных...

📁 Создана директория: backups
📝 Файл резервной копии: backups/yms_backup_20241224_135600.sql

🔄 Проверка состояния контейнера БД...
✅ Проверка состояния контейнера БД - успешно
🔄 Создание дампа базы данных...
✅ Создание дампа базы данных - успешно
📊 Размер резервной копии: 2.45 МБ
🔄 Сжатие резервной копии...
✅ Сжатие резервной копии - успешно
📦 Размер сжатой копии: 0.85 МБ

🎉 Резервная копия создана успешно!
📁 Файлы:
   - backups/yms_backup_20241224_135600.sql
   - backups/yms_backup_20241224_135600.sql.gz
```

### `restore_database.py`
Восстанавливает базу данных из резервной копии.

**Что делает:**
- Показывает список доступных резервных копий
- Позволяет выбрать файл для восстановления
- Требует подтверждения (все текущие данные будут заменены)
- Восстанавливает данные из выбранного файла
- Проверяет результат восстановления

**Использование:**
```bash
python restore_database.py
```

**Пример вывода:**
```
🔄 Восстановление базы данных из резервной копии
⚠️  ВНИМАНИЕ! Все текущие данные будут заменены!

📁 Доступные резервные копии:
   1. yms_backup_20241224_135600.sql (2.45 МБ)
   2. yms_backup_20241224_134500.sql (2.30 МБ)

Введите номер файла для восстановления (или 'q' для выхода): 1
📁 Выбран файл: yms_backup_20241224_135600.sql

Вы уверены? Все текущие данные будут потеряны (yes/no): yes

🔄 Проверка состояния контейнера БД...
✅ Проверка состояния контейнера БД - успешно
🔄 Восстановление базы данных...
✅ Восстановление базы данных - успешно

🎉 База данных восстановлена успешно!

🔍 Проверка восстановления:
🔄 Количество пользователей...
✅ Количество пользователей - успешно
   Вывод: count
-------
      5
🔄 Количество бронирований...
✅ Количество бронирований - успешно
   Вывод: count
-------
     12
```

## Структура файлов

```
backups/
├── yms_backup_20241224_135600.sql      # Обычный SQL дамп
├── yms_backup_20241224_135600.sql.gz   # Сжатый дамп
└── ...
```

## Безопасность

- **Резервное копирование:** Безопасно, не влияет на работу системы
- **Восстановление:** **УДАЛЯЕТ ВСЕ ТЕКУЩИЕ ДАННЫЕ** - требует явного подтверждения
- Все скрипты проверяют состояние контейнера БД перед выполнением

## Требования

- Docker и docker-compose должны быть установлены
- Контейнер БД должен быть запущен (`docker compose up -d`)
- Python 3.x для запуска скриптов
- `gzip` для сжатия файлов (обычно предустановлен в Linux/Mac)

## Автоматизация

Для автоматического резервного копирования можно добавить в cron:

```bash
# Ежедневное резервное копирование в 2:00
0 2 * * * cd /path/to/project && python backup_database.py
```

## Восстановление в новом окружении

Если нужно восстановить данные в новом окружении:

1. Запустите проект: `docker compose up -d`
2. Дождитесь инициализации БД
3. Запустите восстановление: `python restore_database.py`

## Troubleshooting

### Ошибка "Контейнер БД не запущен"
```bash
docker compose up -d
```

### Ошибка прав доступа
```bash
chmod +x backup_database.py restore_database.py
```

### Ошибка при восстановлении
- Убедитесь, что файл резервной копии не поврежден
- Проверьте, что контейнер БД пустой (или согласны на перезапись)

## Важные замечания

- **Всегда делайте резервную копию перед восстановлением!**
- Храните резервные копии в безопасном месте
- Регулярно проверяйте возможность восстановления
- Тестируйте процесс на копии production данных